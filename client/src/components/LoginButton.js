import React from 'react';

const LoginButton = () => {
    return (
        <a href="/auth/patreon" className="px-4 py-2 bg-blue-500 hover:bg-blue-700 text-white font-bold rounded">
            Login with Patreon
        </a>
    );
};

export default LoginButton;
